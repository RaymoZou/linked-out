// TODO: figure out why colors are being properly set
// export default function Button({ color, onClick, children }) {
//     const buttonClass = `transition bg-${color}-500 hover:bg-${color}-600 text-lg text-white font-bold p-2 rounded`;
//     return <button className={buttonClass} onClick={onClick ? onClick : null}>{children}</button>;
// };
